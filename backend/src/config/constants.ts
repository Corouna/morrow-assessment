// No auth system in this exercise — every request acts as this single
// hardcoded demo user. This is a deliberate, documented shortcut (see
// README "Assumptions"), not something silently skipped.
export const DEMO_USER_ID = 1;
